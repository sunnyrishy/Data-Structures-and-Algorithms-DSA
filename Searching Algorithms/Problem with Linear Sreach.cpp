For the Sorted array it takes - n steps.

For the unsorted array it takes - n steps.


  Our need is to take only log(N) steps for the sorted search.

  This can't be done using Linear Search, So Binary Search is used to search the sorted array in log(N) steps
